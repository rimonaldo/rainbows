// window header
    // active tab

// color picker
    // main color 
    // color setting -> s,l  

// harmony container
    // [harmony,setHarmony] -> useHarmony
        // type
        // colors (1-3)
        
// palette
        // shade -> light bg | light text
        // accent -> contrast
        // light
    // main color -> eye-catching but not harsh
        // dark
        // accent -> optional accent
        // shade -> dark bg | dark text
        

// functuinal missions 
// * leave clean code and commits

// color setting (saturation and luminance)

// state managment -> apply color class for each color 

// [harmony,setHarmony] -> useHarmony (type and colors)

// palette handling (to be continued...)



why to set useColorContext


color

setColor(hex)
rgb = {...color.rgb , newVal}
setColor(convervToHex('rgb',rgb))
setColor(convervToHex('hsl',hsl))
setColor(convervToHex('hsv',hsv))



setHarmony(color.hex)

addPaletteColor(hex)
removePaletteColor(hex)
updatePaletteColor(hex)

setPalette(color)